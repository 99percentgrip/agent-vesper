[package]
name = "vesper-harness"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
regex.workspace = true
sha2.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio = { workspace = true, features = ["process"] }
tempfile.workspace = true
url.workspace = true
futures-util.workspace = true
vesper-agent = { path = "../vesper-agent", version = "=0.21.3" }
vesper-checkpoints = { path = "../vesper-checkpoints", version = "=0.21.3" }
vesper-config = { path = "../vesper-config", version = "=0.21.3" }
vesper-domain = { path = "../vesper-domain", version = "=0.21.3" }
vesper-mcp = { path = "../vesper-mcp", version = "=0.21.3" }
vesper-memory = { path = "../vesper-memory", version = "=0.21.3" }
vesper-runtime = { path = "../vesper-runtime", version = "=0.21.3" }
vesper-sandbox = { path = "../vesper-sandbox", version = "=0.21.3" }
vesper-sessions = { path = "../vesper-sessions", version = "=0.21.3" }
# VRO-14 PR-5: the web tools' pure surfaces (perception pipeline, egress
# policy, action vocabulary) live in vesper-web; the transport stays behind
# the port trait and is supplied by the composition boundary.
vesper-web = { path = "../vesper-web", version = "=0.21.3" }
vesper-web-fetch = { path = "../vesper-web-fetch", version = "=0.21.3" }

[features]
# VRO-13 PR-6: the daemon's single-writer flock discipline. Unix-only

# Forwards to vesper-sandbox's docker backend so hosts can enable it
docker = ["vesper-sandbox/docker"]

[dev-dependencies]
tempfile.workspace = true
# VRO-13 PR-8: the cross-feature e2e fixture composes the firewall holder
# (vesper-policy), the provider seam (vesper-provider), and the deterministic
# fake provider (vesper-testkit) — test-only, mirroring vesper-agent/tests.
vesper-policy = { path = "../vesper-policy", version = "=0.21.3" }
vesper-provider = { path = "../vesper-provider", version = "=0.21.3" }
vesper-testkit = { path = "../vesper-testkit", version = "=0.21.3" }

[lints]
workspace = true
