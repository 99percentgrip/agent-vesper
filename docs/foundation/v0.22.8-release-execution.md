# v0.22.8 release execution: voice push-to-talk repair

Date: 2026-09-14. Status: **published and verified; local installation
preserved (Alex owns the update)**.
Objective: ship the voice F5-cancel trap fix
([repair record](voice-f5-cancel-trap-repair.md)) so the installed 0.22.6
can be updated to a working push-to-talk.

## Methods and files

- Version synchronized 0.22.7 → **0.22.8** in 28 files / 136 occurrences:
  workspace version, all exact internal `=x.y.z` dependency pins,
  lockfile, `registry/agent.json` (version + 6 archive URLs), and the
  installation-guide version examples.
- Full local gates on the bumped tree before commit.

## Local gate receipts (on the release commit)

- Workspace all-features: **2,326 passed / 0 failed**
- `cargo xtask acceptance`: **23/23** (`68034 ms`)
- clippy `-D warnings`: clean · fmt: clean
- naming-guard: **18 hits, all frozen** · architecture: 27 packages valid

## Exact-commit gates (all four on `4a37af5bd93a1184abc192a64f9afd5915cedb14`)

- pull-request-validation: run `34793505874` — PASS
- web-driver: run `34793505876` — PASS
- five-target-foundation: run `34793505872` — PASS
- msrv: run `34793505877` — PASS

Tag `v0.22.8` created only after those passes (never tag-first).

## Publication receipts

- Release workflow `34795567656`: **PASS**.
- Release: `draft:false`, `prerelease:false`, 16 nonempty assets
  (five platform archives + checksums, two web-driver images + checksums
  + image-ids).
- Unauthenticated `/releases/latest` → **v0.22.8** (publicly discoverable).
- Notes published via `gh release edit --notes-file` (include the
  first-run model-download note).

## Registry receipts (continuous-update contract held)

- [PR #539](https://github.com/agentclientprotocol/registry/pull/539)
  remains OPEN on `agent-vesper/v0.20.51`, updated **in place**.
- Fork manifest updated to 0.22.8 (Contents API commit
  `68cf8a82a19ad2eba5d30a1958bfae0a6f703735`); read-back **byte-identical**
  to `registry/agent.json`.
- PR retitled "Add Agent Vesper v0.22.8" with release evidence. Upstream
  merge not claimed.

## Preserved installation

No installer/updater ran locally; installed binary remains Alex's to
update (0.22.6 → 0.22.8 via native **Check for updates → Install
update**). The voice fix requires this update to reach his machine.

## Deviations

None. Pipeline followed exactly; version 0.22.8 was free.

## Readiness effect

Alex's reported push-to-talk failure is fixed in a published release:
first F5 completes backend preparation instead of being cancellable by
its own toggle key; Del is the documented explicit cancel; the first-run
model download is explained in the release notes.
