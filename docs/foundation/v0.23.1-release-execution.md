# v0.23.1 Release Execution — Vesper Bridge (experimental)

**Date:** 2026-09-18 · **Commits:** `4b7f90e` (feature) → `e7fbc43` (bump) · **Status: local tree ready; push/tag at Alex's 22:00 Manila go**

## Objective

Ship the Vesper Bridge application-control subsystem (VB-PRD-001 Phase 2)
as an **experimental, default-off** feature, per Alex's instruction:
"commit and do release as experimental feature at 10 pm Philippines time."

## Methods / commands

```
cargo fmt --all -- --check                    clean
cargo clippy --workspace --all-targets \
  --all-features -- -D warnings               0 errors
cargo test --workspace --all-features         2,464 passed / 0 failed
cargo xtask acceptance                        23/23 (15.7 s)
cargo xtask architecture                      28 packages validated
cargo xtask naming-guard                      clean (33 frozen)
cargo build --release --workspace \
  --all-features                              1 m 27 s
```

## What shipped (commit 4b7f90e, 247 files)

- **`vesper-bridge` crate**: provider-neutral core — identity/manifests/
  authority epochs, owner-scoped exclusive leases, per-mutation observation
  freshness, bounded core state (records/jobs/journal/pixels), full outcome
  vocabulary, complete session lifecycle, model-independent stop/resume/
  disconnect, emergency input release with explicit settlement, semantic
  duplicate suppression on operation identity.
- **Adapters** (zero new dependencies): MPRIS (state-verified mutations,
  measured behavior guards) and Resolve file-IPC worker channel.
- **Both hosts**: `/bridge` shared command implementation (BR-21), AT-01
  kernel-accounted OS observation, feature-gated default-off.
- **Fixes en route**: `/usage` quota error-envelope honesty (2ai),
  6 test-suite corrections to the H2/H5/§6.6 contracts (tests pinned the
  pre-hardening shapes), scratch-frame cleanup (83 MB), evidence-image
  recompression (139→74 MB).

## Verification evidence

- Full workspace all-features: **2,464 / 0** (166 test suites).
- All gates re-run on the bumped 0.23.1 tree: fmt, bridge suites (12 ok),
  release build green.
- v0.23.0 was already published (`5a3ce20`), so this is **0.23.1**.

## The 22:00 Manila sequence (remaining)

1. `git push origin main` (both commits)
2. Wait for canonical CI green on `e7fbc43` (per exact-commit gate contract)
3. `git tag v0.23.1 && git push origin v0.23.1`
4. Release notes: Bridge experimental, default-off, `--features bridge`

## Deviations / unresolved

- Manor Lords lane (2aj): village built via C-construction, **save not
  completed** — UI archaeology incomplete; documented, not blocking this
  release (Bridge adapters unaffected).
- Native acceptance enrollment still unavailable to this session's tool
  surface (harness limitation, documented in 2n audit).

## Readiness effect

Bridge is now committable, versioned, gated, and release-ready as an
experimental feature. No production default changed: `bridge` remains
compile-time off unless built with the feature.
