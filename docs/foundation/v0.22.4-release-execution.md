# v0.22.4 routing preview release execution

Date: 2026-09-13. Status: release preparation; publication pending.
Objective: publish the user-approved opt-in model-assisted routing preview and
refresh the GitHub front page and user documentation.
Owner: [routing PRD](../skill-routing-quality-prd.md).

## Scope and authorization

Alex requested the version bump and release after reviewing the live evaluation
and its remaining quality gaps, then explicitly requested README and documentation
updates. This authorizes preview distribution. It does not change Standard as the
default or the routing-quality HOLD, authorize more live provider evaluation, or
authorize replacing the local installation.

Release work runs in `/tmp/vesper-routing-quality`, on the existing isolated branch.
Implementation checkpoint: `6659335b0b5e4bab136ea230f571c5451a00c5dc`.
GitHub main was `75c1a5508055f0a373f9ca335b64c414dc97a393`; the original checkout
remains at GLM score-floor PR-1 `5cf5835f78d8e779443188c3490172d7736a589a` with
its existing uncommitted work preserved. The release includes the already stacked
exemplar and PR-1 anchor commits; it does not claim the pending PR-2/PR-3 repair.

## Methods and files

- Synchronize workspace version, exact internal dependency pins, lockfile,
  registry version/archive URLs and pinned installation examples to 0.22.4.
- Update `README.md`, `docs/README.md`, `docs/using-vesper.md` and new
  `docs/skills.md` with concrete activation, usage, preservation and rollback guidance.
- Update current migration status, owning DOX, PRD and evidence index.
- Run canonical local verification; push the version commit to GitHub main;
  require successful canonical, MSRV, five-target foundation and contained
  web-driver workflows for that exact commit before creating its immutable tag.
- Publish only through `release.yml`, verify public asset/checksum inventory and
  latest-release discovery, and update existing registry PR #539 in place.
- Compare installed-binary hashes and original-workspace dirty-file hashes to
  `/tmp/vesper-0224-preservation.json`. Do not run an installer or plugin update.

## Implementation evidence and limits

[Implementation report](skill-routing-model-assistance-execution.md) and
[independent live evaluation](skill-routing-model-live-evaluation.md) retain the
exact pre-bump source identity and all failures. Offline verification passed 2,305
workspace tests and 23 acceptance cases. The live 205-case regression found the
expected skill in 89/95 positives, activated no skill on all 60 no-skill cases,
and selected forbidden siblings in 2/30 cases. Five provider errors used fallback;
12 resource contexts remain unsupported by the diagnostic.

This is an inspected-corpus GLM-5.3/Linux regression, not a universal accuracy
claim. Fresh holdout, all-size model-assisted quality, broader host adversarial
coverage and completed score-floor integration remain outside this release's
completion claims. Standard remains default; model assistance requires opt-in.

## Release verification

Exact version-commit, local verification, CI, publication and registry receipts
will be appended as they complete. No pending gate counts as successful.

## DOX and readiness

Root and affected crate/app/registry/documentation contracts were consulted.
Version-only manifest changes do not change crate ownership or dependency direction,
so their owning contracts remain unchanged. Documentation ownership now includes
the user skill guide and this release record; affected indexes and the PRD link them.
Publication is pending. User installation and interactive acceptance remain Alex's step.
