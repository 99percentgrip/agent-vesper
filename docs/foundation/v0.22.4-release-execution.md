# v0.22.4 routing preview release execution

Date: 2026-09-13. Status: COMPLETE — published, verified, registry PR updated; local installation preserved.
Objective: publish the user-approved opt-in model-assisted routing preview and
refresh the GitHub front page and user documentation.
Owner: [routing PRD](../skill-routing-quality-prd.md).

## Scope and authorization

Alex requested the version bump and release after reviewing the live evaluation
and its remaining quality gaps, then explicitly requested README and documentation
updates. This authorizes preview distribution. It does not change Standard as the
default or the routing-quality HOLD, authorize more live provider evaluation, or
authorize replacing the local installation.

Release work runs in `/tmp/vesper-routing-quality`, on the existing isolated branch.
Implementation checkpoint: `6659335b0b5e4bab136ea230f571c5451a00c5dc`.
GitHub main was `75c1a5508055f0a373f9ca335b64c414dc97a393`; the original checkout
remains at GLM score-floor PR-1 `5cf5835f78d8e779443188c3490172d7736a589a` with
its existing uncommitted work preserved. The release includes the already stacked
exemplar and PR-1 anchor commits; it does not claim the pending PR-2/PR-3 repair.

## Methods and files

- Synchronize workspace version, exact internal dependency pins, lockfile,
  registry version/archive URLs and pinned installation examples to 0.22.4.
- Update `README.md`, `docs/README.md`, `docs/using-vesper.md` and new
  `docs/skills.md` with concrete activation, usage, preservation and rollback guidance.
- Update current migration status, owning DOX, PRD and evidence index.
- Run canonical local verification; push the version commit to GitHub main;
  require successful canonical, MSRV, five-target foundation and contained
  web-driver workflows for that exact commit before creating its immutable tag.
- Publish only through `release.yml`, verify public asset/checksum inventory and
  latest-release discovery, and update existing registry PR #539 in place.
- Compare installed-binary hashes and original-workspace dirty-file hashes to
  `/tmp/vesper-0224-preservation.json`. Do not run an installer or plugin update.

## Implementation evidence and limits

[Implementation report](skill-routing-model-assistance-execution.md) and
[independent live evaluation](skill-routing-model-live-evaluation.md) retain the
exact pre-bump source identity and all failures. Offline verification passed 2,305
workspace tests and 23 acceptance cases. The live 205-case regression found the
expected skill in 89/95 positives, activated no skill on all 60 no-skill cases,
and selected forbidden siblings in 2/30 cases. Five provider errors used fallback;
12 resource contexts remain unsupported by the diagnostic.

This is an inspected-corpus GLM-5.3/Linux regression, not a universal accuracy
claim. Fresh holdout, all-size model-assisted quality, broader host adversarial
coverage and completed score-floor integration remain outside this release's
completion claims. Standard remains default; model assistance requires opt-in.

## Release verification

Release commit: `fdac018d31ef790d528132f649b99a31a9fa5fed`. Immutable annotated tag:
`v0.22.4`, created only after all four successful push runs below.

| Exact-commit gate | Result |
| --- | --- |
| [ci.yml](https://github.com/99percentgrip/agent-vesper/actions/runs/34743527407) | PASS |
| [msrv.yml](https://github.com/99percentgrip/agent-vesper/actions/runs/34743527438) | PASS |
| [platform-foundation.yml](https://github.com/99percentgrip/agent-vesper/actions/runs/34743527403) | PASS |
| [web-driver.yml](https://github.com/99percentgrip/agent-vesper/actions/runs/34743527385) | PASS |

Local v0.22.4 verification passed formatting, Clippy with warnings denied, 2,305
workspace tests, doctests, fixture/contract/architecture checks, then stopped at
the documented narrative naming issue. After correcting that narrative, naming,
GLM/runtime/ACP/session conformance and all 23 exact acceptance cases passed.
The platform-fixture correction passed all 14 composition tests locally. The
final canonical CI above independently passes the complete `cargo xtask verify`
sequence, mutation gate, documentation structure and supply-chain policy.
All five target families pass their complete platform gates; MSRV is Rust 1.88.

Local receipts: `/tmp/vesper-0224-verify.log`,
`/tmp/vesper-0224-remaining-gates.log`, `/tmp/vesper-0224-acceptance.log`,
`/tmp/vesper-0224-portable-fixture.log`. The documented user-guide refresh passed
75 relative link-target checks. Registry schema and full entry/icon validation
passed offline and again after publication with all five archive URLs checked.
The schema, validator and utility module byte-match current upstream.

Release packaging run:
[34745021985](https://github.com/99percentgrip/agent-vesper/actions/runs/34745021985).
Result: PASS — all five builds and publication succeeded.


## Publication and download receipts

[Agent Vesper v0.22.4](https://github.com/99percentgrip/agent-vesper/releases/tag/v0.22.4)
is the published latest release, not a draft or GitHub prerelease. The product's
model-assisted feature remains an opt-in preview. Release notes link the user
Skills guide, activation steps, scoped quality evidence and updater workflow.

`python3 /tmp/vesper-0224-verify-assets.py` verified:

- Exactly 16 uploaded assets: five platform bundles with checksums, plus two
  browser-image archives, their checksums and immutable image IDs.
- All seven checksum files agree with GitHub's asset SHA-256 digests.
- The downloaded Linux x86_64 bundle matches its checksum. Its ACP and TUI report
  `0.22.4` when run with `--version` in `/tmp`, without installation. The probe
  captures both stdout and stderr because ACP prints its version to stderr.
- ACP, TUI, fetch helper, sandbox supervisor and bundled browser files are present.
- All 488 bundled skill files byte-match the preserved source-library baseline.
- The bundled browser archive, checksum and image ID match the published,
  exact-commit-tested Linux x86_64 driver image.
- GitHub's latest-release endpoint returns `v0.22.4`, making it discoverable by
  the native updater. Alex's actual updater interaction remains unexecuted here.

Only the Linux bundle received this local content/version inspection; other
platforms have their successful native CI/build gates and published checksum
verification, not a claimed local installation or interactive test.

| Archive | Verified published SHA-256 |
| --- | --- |
| `agent-vesper-acp-darwin-x86_64.tar.gz` | `15dc143809b61c03352d1ebe5d4d2e9d56f3db8a32b389a1debfb33442af857a` |
| `agent-vesper-acp-darwin-aarch64.tar.gz` | `463c8bebdf3aace8709f48bdd14f9073a2566c011e18d4982b19a3a309f2a682` |
| `agent-vesper-acp-linux-x86_64.tar.gz` | `0461c27e47361cd74082175d4ffdf0cdb7c602b1fb2360d5ca77f599195b6517` |
| `agent-vesper-acp-linux-aarch64.tar.gz` | `8ce7a2b82b2105c1565b853290e06ebadbefec9df63df5c507f61427421e3add` |
| `agent-vesper-acp-windows-x86_64.zip` | `5ce415057b5a99bde7b3c359e05465986a1857a5c60ea4a292bd122e3648fb1d` |
| `vesper-web-driver-linux-x86_64.tar.gz` | `30e83df392e717e9559e5f460eb83f10a5e9e17d18230c8bf545d865585b3f5b` |
| `vesper-web-driver-linux-aarch64.tar.gz` | `b51b4edc6f694ec639b571e9a0a662ebf4e7409e361a194f52582f2484339bd3` |

Receipt files: `/tmp/vesper-0224-assets-verification.log`,
`/tmp/vesper-0224-assets/release.json`, `/tmp/vesper-0224-assets/checksums.json`.

## Registry and preservation

Existing [registry PR #539](https://github.com/agentclientprotocol/registry/pull/539)
is updated in place on `agent-vesper/v0.20.51`; it remains open for upstream review.
Registry commit: `1824fdd3bda87d38cda73e656d0b4d7e538c0fff`.
Both remote `agent.json` and `icon.svg` byte-match the local canonical files.
The current upstream `process_entry` validator passes with URL checks enabled.
No replacement PR was created.

Final hash checks against `/tmp/vesper-0224-preservation.json` and the existing
library/GLM baselines confirm: original checkout HEAD and ten dirty files
unchanged, both installed executables unchanged, all 488 library files unchanged,
and all five protected GLM files unchanged. No installer or plugin update ran.
The original checkout remains at `5cf5835`; GitHub main contains the release work.
Future GLM integration must account for this remote advance without overwriting
its preserved local changes.

## DOX and readiness

Root and affected crate/app/registry/documentation contracts were consulted.
Version-only manifest changes do not change crate ownership or dependency direction,
so their owning contracts remain unchanged. Documentation ownership now includes
the user skill guide and this release record; affected indexes and the PRD link them.
Publication and this release work unit are complete. User installation and
interactive updater acceptance remain Alex's step. The routing-quality HOLD and
pending score-floor PR-2/PR-3 remain explicit; release is not PRD completion.
This documentation closeout follows the immutable release tag. Its local naming,
link and whitespace checks do not change the exact-commit CI identity above.

## Registry compatibility repair before tagging

Upstream schema validation found the existing manifest missing the now-required
`license_url`. The release manifest now links the Apache-2.0 license and lists
all three real adapters, including native OpenAI. Validation uses the upstream
`agent.schema.json` and isolated Python jsonschema 4.26.0. The initial version
commit `45decaa4a0c686e062f571d7ca0cc507e25901d6` is superseded before tagging;
all four exact-commit gates passed again on the final corrected commit.

The upstream entry validator also requires a 16×16 monochrome `icon.svg`; the
previous entry lacked one. A Vesper icon is now included and validated with the
manifest. URL checks were deferred until publication, then passed. Local verification reached the naming guard after all
workspace tests passed, then rejected two branded skill IDs in narrative evidence.
The narrative now references their unchanged raw case receipts; no baseline,
labels, results or library entries were changed. The guard and remaining gates
passed before tagging.

## Cross-platform fixture correction

Five-target run [34742214416](https://github.com/99percentgrip/agent-vesper/actions/runs/34742214416)
failed on both macOS architectures and Windows in two selector composition tests.
Their shared fixture prepared a Linux routing query, while the native resolver
correctly rechecked the actual host OS and withheld the stale selection. The
fixture now uses `std::env::consts::OS`, matching both production hosts. No
production eligibility check or expected selection was weakened. Linux focused
regression and fresh five-target CI passed; commit `49f8f0d` was not tagged.

## Windows frozen-corpus checkout correction

The corrected selector tests passed on Windows, then run
[34742605092](https://github.com/99percentgrip/agent-vesper/actions/runs/34742605092)
found the frozen corpus digest changed by Git CRLF checkout conversion. Reproducing
LF-to-CRLF conversion locally yields exactly the Windows failure digest
`ad653e81fb25d08106708b189e23b3e70afc9db949fdbe347dd93881ce4440c1`.
`.gitattributes` now pins routing JSON/JSONL evidence to LF. The frozen
`79ef89a4934a0e2c61aa08d4e2de6a3efd0272c18f1ee3e9507271d5c6111275`
corpus hash, cases and test assertions remain unchanged. A temporary checkout
with `core.autocrlf=true` verified the original digest before CI. Windows then
passed the frozen-corpus test and complete platform gate. Commit `191954c` was
not tagged; all four fresh gates passed on `fdac018`.
