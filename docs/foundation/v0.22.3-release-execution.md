# v0.22.3 visual upgrade release execution

Date: 2026-09-13. Status: published and verified; installed 0.22.2 preserved.
Objective: publish the [visual upgrade](../output-visual-upgrade-prd.md) so Alex can
upgrade the installed 0.22.2 through its native confirmed installer.

## Methods and files

- Synchronize workspace version, exact internal Cargo dependency pins, lockfile,
  registry version/archive URLs and pinned installation examples to 0.22.3.
- Commit the visual implementation and release metadata; push main; require all
  four exact-commit push workflows before creating the immutable release tag.
- Publish through release.yml, verify public release assets and update the existing
  registry PR #539 on its existing branch. Never replace that PR.
- Preserve installed binaries and configuration. Snapshot installed binary hashes
  before/after publication; run no local installer or plugin update.
- Verify unauthenticated latest-release discovery, release asset metadata and exact
  registry manifest bytes; record final receipts below.

## Implementation evidence

[Visual upgrade execution](output-visual-upgrade-execution.md) records production
changes, 2,247 workspace tests, final 240 renderer tests, 924 MSRV tests, 23 exact
acceptance cases, real terminal regression and renderer captures. Release gates
must validate the versioned commit independently; prior tests are not substitutes.

## Installed payload baseline

Read-only SHA-256 before release:

- TUI: `bd40d0a4c26733354e8a8c1ac1f970bca2397411c80e63fec0353d999ee8bbef`.
- ACP: `ef0672aa4b6425fe39b356c7fd4b3ce34d3dbdf1fcaca14151348ea555cc7525`.

Paths are under `/home/Alex/.local/share/agent-vesper/`. The local installed version
stays 0.22.2; Alex owns the 0.22.2 → 0.22.3 updater acceptance step.

## Evidence and remaining acceptance

Publication and registry verification completed. Native Windows/macOS visual
execution and the user's real updater/install test remain unexecuted.
Passing cross-platform compilation/tests does not claim visual pixel identity.

## DOX closeout

Root, composition/crate, registry and documentation contracts were consulted.
Manifest-only version edits do not change ownership, dependencies or workflows;
those owning AGENTS and child indexes remain unchanged. Foundation ownership,
PRD and evidence index link this report. No local install is authorized here.

## Initial CI failure and repair

Initial version commit `8d95d04ddee81f5042bad1918524562aa0b27064` passed local
`cargo xtask verify` (2,248 workspace tests, 0 failed, 34 ignored; 23 acceptance
cases). Intel macOS job 103591416942 in run 34707984328 failed the existing
`selected_model_reaches_the_real_http_body_instead_of_launch_model` fixture:
`stream.read` at line 1007 returned OS error 35 / WouldBlock. The listener is
nonblocking and macOS inherited that mode on the accepted socket. The fixture
now explicitly sets the accepted stream to blocking before its existing ten-second
read timeout. Real request-body/model assertions are unchanged. No production
transport change or test skip was introduced. All four gates passed again on
the repair commit before tagging, as recorded below.

## Exact-commit release gates

Immutable tag `v0.22.3` resolves to repair commit
`e9364de29b47e85c0822e1d8458ec2c8c43c97bb`. All four push prerequisites completed
successfully before tag creation:

- [Canonical and supply chain](https://github.com/99percentgrip/agent-vesper/actions/runs/34708434169).
- [Rust 1.88 MSRV](https://github.com/99percentgrip/agent-vesper/actions/runs/34708434142).
- [Five-target foundation](https://github.com/99percentgrip/agent-vesper/actions/runs/34708434159).
- [Contained web-driver acceptance](https://github.com/99percentgrip/agent-vesper/actions/runs/34708434193).

The repaired loopback test passed locally (1 passed, 0 failed) and in the fresh
platform matrix, including Intel macOS. Commands: `cargo xtask verify`, focused
`cargo test -p agent-vesper-tui selected_model_reaches_the_real_http_body_instead_of_launch_model`,
`gh run list`, `gh run view`, `git tag -a v0.22.3 <commit>`, and
`git push origin v0.22.3`. The release workflow independently checked these gates.

## Publication and registry receipts

- [Release workflow 34709443693](https://github.com/99percentgrip/agent-vesper/actions/runs/34709443693)
  completed successfully: exact-commit gate check, all five package builds and
  publication. [v0.22.3](https://github.com/99percentgrip/agent-vesper/releases/tag/v0.22.3)
  is public, latest, neither draft nor prerelease, with 16 nonempty assets:
  five platform archives and checksums, plus two driver archives, checksums and IDs.
- `gh release view v0.22.3 --json tagName,isDraft,isPrerelease,assets,url` verified
  the asset inventory. `gh release edit --notes-file` supplied user-facing notes.
  An unauthenticated Python urllib request to GitHub's `/releases/latest` returned
  `v0.22.3` with all 16 assets; the published update is discoverable without login.
- [Registry PR #539](https://github.com/agentclientprotocol/registry/pull/539)
  remains open on `agent-vesper/v0.20.51`. GitHub Contents API updated the existing
  manifest in fork commit `bf6520a5966e76c49bdd41d68d5644bbf9625cc9`; a read-back
  base64 decode matched `registry/agent.json` byte for byte. PR title/body now
  describe 0.22.3 and the exact release evidence. Upstream merge is not claimed.
- Both installed binaries still report `0.22.2`; SHA-256 values after publication
  exactly match the baseline above. No installer or plugin update ran locally.
- Documentation receipts are committed after publication; the immutable release
  tag remains on `e9364de29b47e85c0822e1d8458ec2c8c43c97bb`. Markdown relative links
  resolve and `git diff --check` passes. Documentation-only receipts do not change
  the tested release payload or require a replacement tag.

## Readiness effect

Alex can now use installed 0.22.2: **Check for updates → Install update → confirm**,
follow progress, then close and reopen Vesper to inspect 0.22.3. This publishes
what is needed for that test; it does not claim the user's interactive installation
or subjective screenshot acceptance already passed. No release scope was dropped.

## Subsequent user updater acceptance

After publication, Alex supplied five screenshots showing the native Linux
0.22.2 → 0.22.3 flow: installation confirmation, archive download, installed binary
versions and driver setup progress, **Update installed**, and the reopened welcome
screen displaying **v0.22.3**. This is user-supplied evidence of successful native
update and restart on that Linux setup. Earlier preserved-install receipts above
record the state before Alex performed the update. It does not establish native
Windows/macOS updater acceptance or the coding-output visual comparison.
