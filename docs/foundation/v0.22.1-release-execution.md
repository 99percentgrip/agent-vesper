# v0.22.1 Release Execution Record (Ranker-Hardening Audit Fix)

- **Work unit:** release pipeline for the final-audit fix (A1)
- **Status:** COMPLETE — released, registry updated, local install updated
- **Date:** 2026-09-12
- **Contents:** A1 overhead-metric tokenizer fix (`a9605e1`) + version bump
  to 0.22.1 (`1b221dc`)

## 1. Exact-commit release sequence (receipts)

| Step | Evidence |
|---|---|
| Local gates on bumped tree | workspace **2,220/0** · acceptance **20/20** · clippy `-D warnings` clean · fmt clean · architecture 27 · naming-guard clean (18 frozen, zero erosion) |
| Version commit | `1b221dc` `chore(release): v0.22.1` pushed |
| Push workflows on exact commit | pull-request-validation 34695780801 ✅ · msrv 34695780805 ✅ · web-driver 34695780793 ✅ · five-target-foundation 34695780796 ✅ (each `gh run watch --exit-status` = 0) |
| Tag | `v0.22.1` → `1b221dc`, pushed only after all four green |
| Release run | 34696509284 ✅ — **16 assets** (5 archives + sidecars + both Linux driver images) |
| Published binary check | downloaded linux-x86_64 archive from the release, extracted, executed: **`agent-vesper-acp 0.22.1`** |
| Registry PR #539 | updated in place on the same branch (head `8138e28`), title → v0.22.1 via REST PATCH |

## 2. Local install update

```
$ AGENT_VESPER_VERSION=0.22.1 sh scripts/install.sh
$ agent-vesper-acp --version  → agent-vesper-acp 0.22.1
$ agent-vesper-tui --version  → agent-vesper-tui 0.22.1
```

Both installed binaries on PATH now carry the audit fix.

## 3. Version surfaces

Workspace version, exact internal pins (0 stragglers verified by grep),
`registry/agent.json` (version + 5 archive URLs → v0.22.1),
`docs/installation.md` pinned examples.

## 4. Deviations

None. The A1 fix itself and its evidence live in
`ranker-hardening-final-audit.md` (shipped in `a9605e1`); this record covers
the release vehicle only.

## 5. Readiness effect

v0.22.0's single known latent defect (eval overhead metric tokenizer) is
fixed in shipped binaries. The installed local agent runs the audited
ranker and metric. Registry consumers can install 0.22.1.
