# v0.22.9 release execution: voice silence-hallucination guard

Date: 2026-09-14. Status: **published and verified; local installation
preserved (Alex owns the update)**.
Objective: ship the VAD silence guard
([execution record](voice-vad-silence-guard-execution.md),
[PRD](../voice-trailing-silence-vad-prd.md)) fixing the trailing-silence
hallucination Alex observed on his first real long dictation.

## Methods and files

- Version synchronized 0.22.8 → **0.22.9** in 28 files / 130 occurrences
  (workspace version, exact internal pins, lockfile, registry manifest
  version + 6 archive URLs, installation-guide examples).
- Full local gates on the bumped tree before commit.

## Local gate receipts (on the release commit)

- Workspace all-features: **2,326 passed / 0 failed**
- `cargo xtask acceptance`: **23/23** (`62542 ms`)
- clippy `-D warnings`: clean · fmt: clean · naming-guard: 18 frozen ·
  architecture: 27 packages valid

## Exact-commit gates (all four on `342f5b128480651456a5e121c88f8e8c84afab57`)

- pull-request-validation: run `34832748575` — success (26m53s)
- web-driver: run `34832748308` — success (8m1s)
- five-target-foundation: run `34832748310` — success (~44m)
- msrv: run `34832748382` — success (23m28s)

Tag `v0.22.9` created only after all four passed (never tag-first).

## Publication receipts

- Release workflow `34836496505`: **completed success**.
- Release: `draft:false`, `prerelease:false`, **16 nonempty assets**.
- Unauthenticated `/releases/latest` → **v0.22.9**.
- Notes published via `gh release edit --notes-file`.

## Registry receipts (continuous-update contract held)

- [PR #539](https://github.com/agentclientprotocol/registry/pull/539)
  remains OPEN on `agent-vesper/v0.20.51`, updated **in place**
  (Contents API commit `3b1a7bd4add0aef40a848074a95b234ca44960b4`).
- Read-back **byte-identical** to `registry/agent.json`; PR retitled
  "Add Agent Vesper v0.22.9" with evidence. Upstream merge not claimed.

## Preserved installation

No installer/updater ran locally. Alex updates 0.22.8 → 0.22.9 via the
native updater at his discretion.

## Deviations

None. Pipeline followed exactly; version 0.22.9 was free.

## Readiness effect

Long dictations ending in silence stay clean end to end: the VAD guard
filters quiet before the model sees it, removing the repeated-token
artifact Alex measured ("1 min" ×77). His next real dictation is the
acceptance surface.
