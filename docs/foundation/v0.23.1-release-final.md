# v0.23.1 Release — Final Report (published)

**Date:** 2026-09-18 · **Status: PUBLISHED** · **Tag:** `v0.23.1` @ `a47fc1c`

## Outcome

**Agent Vesper v0.23.1 — Vesper Bridge (experimental) — is live.**

## Exact sequence executed

1. **Push** `main`: `5a3ce20..220685b` (feature + bump + docs), Alex's "go".
2. **CI on `220685b`**: canonical, MSRV, web-driver green; five-target green.
3. **Tag `v0.23.1`** → release workflow ran → **FAILED at "Validate release
   metadata"**: `registry/agent.json` still said `0.23.0` (the gate requires
   tag == workspace == registry). No artifacts had built.
4. **Fix**: bumped `registry/agent.json` → `0.23.1`, deleted the un-published
   remote tag (nothing had shipped — no users affected), pushed
   `a47fc1c`.
5. **CI on `a47fc1c`**: all 4 workflows green (~31 min).
6. **Re-tagged `v0.23.1`** → release workflow **SUCCESS** (29 min).
7. **Registry PR #539** (continuous-update contract, same branch
   `agent-vesper/v0.20.51`, never closed): payload updated to v0.23.1
   with all five platform URLs, pushed in place.

## Evidence receipts

```
Release: Agent Vesper v0.23.1 — published 2026-09-17T15:43:32Z, not a draft
Assets:  5 platforms (darwin x2, linux x2, windows) + sha256 + 2 web-driver
         images with image-ids — 14 artifacts total
CI:      4/4 green on both 220685b (feature) and a47fc1c (tagged) commits
PR:      agentclientprotocol/registry#539 OPEN, latest commit "agent-vesper
         v0.23.1" — updated in place per the continuous-update contract
```

## Deviations

- One failed release attempt (registry manifest version) — caught by the
  gate exactly as designed, fixed, re-verified, re-tagged. Total added
  time ~45 min.
- Registry PR branch name retains the historical `v0.20.51` name (the
  contract pins the branch, not its name's literal version).

## Readiness effect

Vesper Bridge ships experimental/default-off on all five platforms with
pinned, checksummed artifacts. `--features bridge` enables it; production
defaults unchanged.
