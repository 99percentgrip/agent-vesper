# v0.22.2 release execution

Date: 2026-09-12–13 (Asia/Manila). Status: published; registry updated;
local installation intentionally left to the user.
Objective: commit and publish the [Settings repair](../settings-and-update-prd.md),
bumping 0.22.1 to 0.22.2. User explicitly reserves local TUI/plugin installation
and update testing; do not run the installer against their installed bundle.

## Methods and files

- Bump workspace version, internal exact path dependency pins, Cargo.lock,
  registry version/archive URLs and pinned installation examples.
- Commit the prior repair plus release metadata and push main; require canonical,
  MSRV, five-target foundation and contained-driver acceptance on that exact SHA
  before creating the immutable tag. Publish through release.yml only.
- Verify public asset metadata and update the existing registry PR #539 in place.
- Record actual workflow/asset evidence here after publication.

## Evidence and unresolved items

The [implementation report](settings-and-update-execution.md) contains prior
Linux tests and remaining native updater acceptance limits. The four release gates passed on `c9c85fee9e209cbec09be81e46ca1407e7f76021`;
publication completed successfully. Native Windows/macOS updater UI and
user installation remain unexecuted. Version 0.22.1's check-only UI cannot install
this new updater itself; installing 0.22.2 enables the workflow for later releases.

## DOX and readiness

Root and owning manifest/documentation contracts were read. Manifest-only bumps
leave local responsibilities and dependency direction unchanged, so their AGENTS
files need no version diary. Foundation ownership, PRD and evidence index link this
record. Publication is authorized; user installation is intentionally excluded.

## CI repair

Initial version commit `b8e14615d86f46920b14d18c31c4dc7b052f50c0` passed local
`cargo xtask verify`. MSRV run 34702362207 failed the existing watcher FD-count
test at line 147: before 6, after 5. Its sibling test opens/closes watched files
concurrently in the same process. Serialized the two test bodies with a shared
mutex while preserving concurrent sweep/input work and the exact equality
assertion. This repairs measurement isolation without weakening the leak gate.
All release prerequisites must pass again on the repair commit before tagging.

## Exact-commit receipts

Version/implementation commit: `b8e14615d86f46920b14d18c31c4dc7b052f50c0`.
Final tested/tagged commit: `c9c85fee9e209cbec09be81e46ca1407e7f76021`.
All below are successful push runs for that exact final commit:

| Required gate | Evidence |
| --- | --- |
| Canonical + supply chain | [34702719118](https://github.com/99percentgrip/agent-vesper/actions/runs/34702719118) |
| Rust 1.88 MSRV | [34702719117](https://github.com/99percentgrip/agent-vesper/actions/runs/34702719117); 23 exact acceptance cases passed in 149,846 ms |
| Five-target foundation | [34702719103](https://github.com/99percentgrip/agent-vesper/actions/runs/34702719103); Linux x86_64/ARM64, macOS Intel/Apple Silicon, Windows x86_64 |
| Contained web driver | [34702719092](https://github.com/99percentgrip/agent-vesper/actions/runs/34702719092); both images and native namespace acceptance |

`git tag -a v0.22.2 c9c85fee9e209cbec09be81e46ca1407e7f76021` and
`git push origin v0.22.2` ran only after checking all four successful push
receipts. [Release run 34703586198](https://github.com/99percentgrip/agent-vesper/actions/runs/34703586198)
published that commit successfully. Local watcher isolation proof: 20 repeated Rust 1.88
runs with two test threads, **40 passed / 0 failed**. The initial failed MSRV
receipt is retained above; it is not counted as a successful gate.

## Publication and registry

[Release v0.22.2](https://github.com/99percentgrip/agent-vesper/releases/tag/v0.22.2)
was published at `2026-09-12T16:04:46Z` (2026-09-13 locally), neither draft nor
prerelease. `gh release view v0.22.2 --json ...` verified **16 nonempty assets**:
five platform archives with SHA-256 sidecars, and two Linux driver archives with
sidecars and immutable image IDs. Release run 34703586198 succeeded, including
all five optimized builds and its publication job. Release notes were updated
with the actual changes, first-upgrade instructions and outstanding UI tests.

Existing [registry PR #539](https://github.com/agentclientprotocol/registry/pull/539)
was updated on the same `agent-vesper/v0.20.51` branch using the Contents API and
REST PR edit. Manifest commit: `366474e0eaf4879fbb1836abc95960569f8b14a7`.
A subsequent read confirmed byte-for-byte equality with `registry/agent.json`.
No PR was closed or replaced. The helper script was not used because its stock
body claims a local installation, which this release explicitly excludes.

## Installed payload preservation

Read-only SHA-256 snapshots before and after publication match:

- Installed `agent-vesper-tui`:
  `aa519b2949856111a318c6795733a1412ac97d4be98aa8df011070ef22f24b01`.
- Installed `agent-vesper-acp`:
  `5e5d2ee8c41671fda6861c0c63ce4cf4067b68b1e04bf11e8e08c1d3298860d4`.

No local installer, plugin updater, provider call or user configuration mutation
was performed. Downloaded-package inspection used `/tmp/vesper-0222-package` only:
`gh release download v0.22.2 --pattern 'agent-vesper-acp-linux-x86_64.tar.gz*'`,
`sha256sum --check agent-vesper-acp-linux-x86_64.tar.gz.sha256` returned **OK**;
extracted ACP and TUI both returned **0.22.2** with `--version`.
No installer was executed. Changed-document relative links and `git diff --check`
passed at report closeout. The final documentation-only receipt commit follows
publication; the immutable release tag remains on the tested code commit.

## Final readiness and limits

The release work unit is published and registry metadata is current. Five-target
CI supplies native Windows compilation/test evidence that the earlier Linux
cross-build could not provide. It does not certify the interactive Windows
updater handoff. Native updater UI/install acceptance on the user's machine,
Windows console handoff and macOS interactive update flow remain open in the
owning PRD; release publication is not a substitute for those acceptance items.
The user expressly chose to perform the local install/update test themselves.
