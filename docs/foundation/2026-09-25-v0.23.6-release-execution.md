# v0.23.6 Critical Executor-Stability Release

## Objective

Publish the accepted classification-D shared command-executor repair as Agent
Vesper v0.23.6 without changing Alex's local installation. The release retains
the exact settlement behavior verified at candidate
`27a3f17bd818294e1a52e0bf194b02ba761e3464` and requires fresh exact-release-
commit canonical, MSRV, five-target foundation and web-driver evidence before
the immutable tag is created.

## Release Content

- Drain stdout and stderr concurrently throughout command execution.
- Bound retained model-visible output independently from transport draining.
- Bound timeout, explicit cancellation and caller-abort settlement.
- Own and clean up command descendants with POSIX process groups and Windows
  Job Objects, including descendant-held output pipes.
- Return truthful partial output, truncation, exit, timeout, cancellation and
  cleanup state without replaying an ambiguous command.
- Keep the production-boundary nine-case settlement matrix enabled on every
  supported native CI runner.

The historical observation that stalls appeared more frequently with OpenAI
was not live-retested because GLM live access is unavailable. No frequency
explanation is inferred. This is an explanatory limitation, not a release
blocker for the provider-neutral executor defect reproduced red and repaired
green.

## Proven Candidate Evidence

The behavior candidate passed all five native jobs in
`five-target-foundation` run `36036088105`: Linux x86_64, Linux ARM64, Windows
x86_64, macOS Intel and macOS Apple Silicon. Each runner executed the portable
settlement matrix behaviorally, including large and mixed streams, truncation
with continued draining, timeout, cancellation, caller abort,
descendant-held pipes, owned-tree cleanup and next-command recovery. The full
red-to-green receipts are in the linked platform-verification record.

## Version and Package Contract

- Workspace and internal path dependency versions: `0.23.6`.
- ACP Registry manifest and all five archive URLs: `0.23.6` / `v0.23.6`.
- Release archives continue to contain ACP, TUI, web-fetch and sandbox
  supervisor binaries, the seed skill library and the exact tested web-driver
  payload for their target.
- GitHub release notes state the executor repair and the unavailable live
  OpenAI/GLM comparison explicitly.
- No model weight, probe artifact, temporary audio or local installation is
  part of this work unit.

## Exact-Commit Gate Ledger

The release commit is pushed to `main` before tagging. Record final evidence
here after the push workflows and publication settle.

| Gate | Result | Receipt |
| --- | --- | --- |
| Canonical CI + supply chain | PENDING | exact release commit required |
| MSRV 1.88 | PENDING | exact release commit required |
| Five-target foundation | PENDING | exact release commit required |
| Web-driver/native host | PENDING | exact release commit required |
| Release metadata/package build | PENDING | tag workflow required |
| Published assets/checksums | PENDING | tag workflow required |
| ACP Registry PR #539 | PENDING | update after verified publication |

## Methods and Local Preparation

- Started from current `origin/main` at v0.23.5 in a dedicated worktree.
- Cherry-picked only the verified repair lineage and its evidence closeout.
- Updated every workspace-owned Cargo version, lockfile package version,
  registry version and release archive URL from 0.23.5 to 0.23.6.
- Updated the tag workflow's release note to describe this repair rather than
  repeat the prior voice-release note.
- Corrected the ACP Registry update template so it names all five release
  workflows and verifies published archive binaries without claiming that the
  user's local installation was replaced.
- Preserved the dirty primary checkout and all unrelated user work.

Local preparation passed:

- `cargo test -p vesper-agent --test command_settlement -- --test-threads=1`
  — 9/9 settlement cases passed.
- `cargo xtask verify` — canonical workspace, strict Clippy, architecture,
  naming, fixtures and acceptance passed; acceptance reported 23 exact cases.
- `cargo +1.88.0 xtask msrv` — passed under the repository MSRV.
- `cargo audit` and `cargo deny --all-features check` — passed.
- `sh scripts/test_install_upgrade.sh` — passed without installing Agent
  Vesper; the isolated installer fixture preserved user state and database
  inode.
- `cargo fmt --all -- --check`, `git diff --check`, installer shell parsing,
  Registry JSON parsing and version/URL consistency checks — passed.

## Deviations and Unresolved Items

- Matched live OpenAI-versus-GLM provider correlation: **NOT EXECUTED — GLM
  live access unavailable**. Fixture-backed adapter parity and the direct
  provider-neutral executor reproduction remain the applicable evidence.
- Historical incident executable identity is not byte-proven. Current source
  independently reproduced the defect at the real `RunCommand` boundary, so
  that provenance limitation does not block this release.
- The first local canonical attempt exhausted the isolated `/tmp` build
  filesystem while writing test artifacts. Generated targets in temporary
  worktrees were removed, the build target was moved to the persistent cache,
  and the complete command then passed. No source failure was hidden.
- Publication, asset verification and registry synchronization remain pending
  until the exact release commit passes all required push workflows.

## Readiness Effect

Release preparation only. Do not mark the issue released until exact-commit
gates, tag workflow, assets/checksums and the required registry update pass.
