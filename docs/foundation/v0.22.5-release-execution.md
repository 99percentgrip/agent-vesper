# v0.22.5 guided dependency setup release

## Objective and status

Alex authorized publication of the implemented guided setup. Status: COMPLETE — published, download-verified, registry PR updated; local installation preserved.
All four exact-commit gates passed before tag `v0.22.5` was pushed. Implementation is `fc254c5`
on the preserved `89bdc07` baseline. The original GLM checkout and installed
executables are outside the release work.

## Methods and files

Bump workspace/Cargo.lock and registry metadata to 0.22.5, refresh README and user
guides, push the version commit to main, require the four exact-commit workflows,
then tag and publish through release.yml. Verify assets and update the existing
registry PR in place. Never install the release on Alex's machine.

## Evidence and limitations

[Implementation evidence](dependency-setup-execution.md) records 2,315 passing
workspace tests, 23 acceptance cases, MSRV 1.88 and real isolated Linux CLI and
Settings readiness. Release-commit remote gates passed as recorded below. Clean-machine
package installation, native macOS/Windows OS approval, reboot and sleep/wake
acceptance remain unexecuted. Publication does not close these PRD gates.

## Readiness and DOX

Guided setup ships as preview with the platform limits in user documentation.
Root/registry contracts remain unchanged: version alignment, exact-commit gates,
continuous registry update and no local installation. Foundation ownership and
evidence index include this report; the PRD links release status separately.

## Exact-commit gates

Release commit: `39d6b9f2fd280e1bbda8e1431c26aa9e1a2e608d`.

| Workflow | Result | Run |
| --- | --- | --- |
| Canonical CI | Success | [34749204521](https://github.com/99percentgrip/agent-vesper/actions/runs/34749204521) |
| Rust 1.88 MSRV | Success | [34749204324](https://github.com/99percentgrip/agent-vesper/actions/runs/34749204324) |
| Five-target foundation | Success, all five platforms | [34749204498](https://github.com/99percentgrip/agent-vesper/actions/runs/34749204498) |
| Browser image acceptance | Success, x86_64 and ARM64 | [34749204336](https://github.com/99percentgrip/agent-vesper/actions/runs/34749204336) |

Both browser jobs passed the new native dependency readiness/cleanup step.
Five-platform CI is build/test evidence; it does not exercise graphical OS
installer approval or a fresh desktop virtualization installation.

Release packaging: [34750470138](https://github.com/99percentgrip/agent-vesper/actions/runs/34750470138), success.

## Publication and registry receipts

[Release v0.22.5](https://github.com/99percentgrip/agent-vesper/releases/tag/v0.22.5)
is published as the stable latest release, with all 16 nonempty assets. The
GitHub latest-release API returns v0.22.5, so native update discovery can find it.
All five packaging jobs and publication passed.

The Linux x86_64 archive downloaded from the successful release job matches its
SHA-256 sidecar. Both extracted executables report 0.22.5 using only `--version`
in a temporary directory. Bundle inventory includes the driver image, checksum,
immutable ID, web helper and namespace supervisor. No installer was invoked.
Receipts: `/tmp/vesper-0225-assets/release.json` and the archive/checksum pair.

Registry PR [#539](https://github.com/agentclientprotocol/registry/pull/539) was
updated in place on its existing branch, commit
`6a2c5abd6e25d81adcefb6292f2c25f673bc5535`. The remote manifest and icon byte-match
the canonical repository files. Schema/icon/version checks and all public archive URL checks passed.

## Deviations and final preservation

An initial local version check caught unchanged exact internal dependency pins.
All workspace pins were aligned to 0.22.5 and third-party lockfile versions kept
unchanged before the version commit; the corrected locked all-feature host check
passed. No failed release commit or replacement tag was published.

The original checkout still matches its recorded HEAD and all pre-existing dirty
file hashes; all 488 skill files match their baseline. Native update testing stays
with Alex. Crate/app/xtask DOX files were intentionally unchanged for numeric
manifest alignment because ownership and runtime contracts did not change.
Publication is complete; missing clean-OS installation acceptance remains open
in the implementation report and PRD.
