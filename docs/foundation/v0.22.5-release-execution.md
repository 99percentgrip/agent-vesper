# v0.22.5 guided dependency setup release

## Objective and status

Alex authorized publication of the implemented guided setup. Release preparation
is in progress; no tag or publication is claimed yet. Implementation is `fc254c5`
on the preserved `89bdc07` baseline. The original GLM checkout and installed
executables are outside the release work.

## Methods and files

Bump workspace/Cargo.lock and registry metadata to 0.22.5, refresh README and user
guides, push the version commit to main, require the four exact-commit workflows,
then tag and publish through release.yml. Verify assets and update the existing
registry PR in place. Never install the release on Alex's machine.

## Evidence and limitations

[Implementation evidence](dependency-setup-execution.md) records 2,315 passing
workspace tests, 23 acceptance cases, MSRV 1.88 and real isolated Linux CLI and
Settings readiness. Release-commit remote gates are pending. Clean-machine
package installation, native macOS/Windows OS approval, reboot and sleep/wake
acceptance remain unexecuted. Publication does not close these PRD gates.

## Readiness and DOX

Guided setup ships as preview with the platform limits in user documentation.
Root/registry contracts remain unchanged: version alignment, exact-commit gates,
continuous registry update and no local installation. Foundation ownership and
evidence index include this report; the PRD links release status separately.
