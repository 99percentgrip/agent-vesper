# v0.22.7 release execution: score-floor initiative

Date: 2026-09-13. Status: **published and verified; local installation
preserved at 0.22.6**.
Objective: release the chunk score-floor initiative through the
exact-commit pipeline after reconciling with the concurrent v0.22.4–v0.22.6
releases.

## The mid-release discovery (deviation, fully resolved)

The first version bump targeted **0.22.4** and its push was rejected:
remote `main` had advanced through three concurrent releases
(**v0.22.4** model-assisted skill routing, **v0.22.5** guided dependency
setup, **v0.22.6** voice controls) while the score-floor initiative ran
locally. Resolution:

1. Dropped the premature bump commit; created `score-floor-backup` branch.
2. Rebased the 7 substantive commits onto `origin/main` (v0.22.6).
3. Resolved 4 conflict clusters honoring the upstream coexistence recon
   (`docs/foundation/skill-routing-coexistence-recon.md`):
   - `evidence-index.md`: both sides' sections, chronological.
   - `skill-routing-quality-prd.md`: upstream's implemented status +
     ledger links won; my embargo-safe citations row kept (conflict
     markers initially missed by a flawed heading-eating regex — caught
     in the post-merge marker sweep, resolved, amended).
   - `dollar_skill_from_prompt`/`explicit_skill_from_prompt` +
     `docs/AGENTS.md`: upstream's invocation-aware parser kept — it
     absorbed the same dollar fix; my local variant superseded.
   - `rank_chunks`: auto-merged; the conjunction gate + `chunk_name_matches`
     landed intact inside upstream's rewritten ranker.
4. Re-bumped to **0.22.7** (next free version): 111 occurrences across
   26 files (workspace version, 100 exact internal pins, lockfile,
   `registry/agent.json` version + 6 archive URLs, installation guide).

Post-merge verification on the rebased tree: **2,324 / 0 failed**
(upstream 2,315 + score-floor tests), 23/23 acceptance, clippy/fmt
clean, naming-guard 18 frozen, architecture 27 packages, **all 11
chunk-routing pins green** (noise-floor, Q1, cross-talk, D3 ladder).

## Exact-commit gates (all four, on the tagged commit)

Commit `574c2249c46efe041a6173875f373319c66b8924` — all passed before
tag creation:

- pull-request-validation: run `34761850348` — PASS
- web-driver: run `34761850345` — PASS
- msrv: run `34761850362` — PASS
- five-target-foundation: run `34761850346` — PASS

## Publication receipts

- Tag `v0.22.7` → `574c224` (annotated, pushed after gates).
- Release workflow `34763599239`: PASS.
- **16 nonempty assets**: five platform archives + checksums, two
  web-driver images + checksums + image-ids.
- `gh release view`: `draft:false`, `prerelease:false`, commit `574c224`.
- Unauthenticated `/releases/latest` → `v0.22.7`, 16 assets (publicly
  discoverable).
- Release notes published via `gh release edit --notes-file`.

## Registry receipts (continuous-update contract held)

- [PR #539](https://github.com/agentclientprotocol/registry/pull/539)
  remains OPEN on its existing branch `agent-vesper/v0.20.51` — updated
  in place, never closed-and-replaced.
- Fork manifest `agent-vesper/agent.json` updated 0.22.6 → **0.22.7**
  (Contents API, commit `a2f1e04168f0ed2bd6036541b3bb796066b27ce4`).
- Read-back base64 decode **byte-identical** to `registry/agent.json`.
- PR title → "Add Agent Vesper v0.22.7"; body describes the initiative,
  gates, and asset verification. Upstream merge not claimed.

## Preserved installation (contract held)

- No installer, updater, or plugin update ran locally.
- Installed `agent-vesper-tui` reports **0.22.6**; the 0.22.6 → 0.22.7
  native update acceptance belongs to Alex.

## DOX closeout

Manifest-only version edits plus release records; no ownership,
dependency, or workflow contracts changed beyond the rebased docs already
recorded in the score-floor execution reports. This record links from
the evidence index; `docs/migration-status.md` and the PRD already
record the initiative as COMPLETE.

## Deviations

- Version 0.22.7 chosen instead of 0.22.4: three versions were published
  concurrently; 0.22.4–0.22.6 taken.
- The rebase superseded the local dollar-parser variant with upstream's
  invocation-aware parser (same fix, evolved call site); behavior
  preserved by upstream's own tests.
- A `git clean -fd` during the recovery removed only untracked build
  artifacts (verified via dangling-blob inspection: vendored dependency
  sources, no work lost); backup branch retained.

## Readiness effect

The score-floor initiative (conjunction gate, delimiter-bounded names,
literal G5, master audit) is now publicly released at v0.22.7 with the
concurrent routing/dependency/voice work merged. Alex can update 0.22.6
→ 0.22.7 through the native installer at his discretion.
