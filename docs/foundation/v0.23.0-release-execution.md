# v0.23.0 release execution

Date: 2026-09-15. Status: **published**.
Type: first minor bump (behavior additions across both hosts).

## Contents

- **Acceptance enrollment freeze fix** (`8d126bd`): nested reviewer stage
  lines stream to both hosts; contract ladder capped at 2 proposals; loud
  bounded failure with do-not-retry guidance. With the **final audit
  repair** (`a6fa19f`): one shared 300 s wall-clock window covering scope
  review AND the contract ladder — AC-3 pinned red-first (pre-fix hang
  reproduced in-process; contract-ladder pin ran 1,208 s before the
  repair).
- **CI-caught clippy repairs** (`c41b600`); local verification now runs
  the exact CI flags.
- **RUSTSEC-2026-0285** (`8e13c40`): rustls 0.23.42 → 0.23.45 (TLS 1.3
  handshake cross-encryption-level acceptance, medium severity).
- Audit-record and docs commits (`bc0ddf1`, `b3f5245`).

## Version sync

0.22.9 → 0.23.0 across 27 files / 138 lines (24 Cargo.tomls, lockfile
27 internal pins, `registry/agent.json` version + all 5 archive URLs,
docs).

## Exact-commit pipeline (never tag-first)

| Gate | Receipt |
|---|---|
| Local gates on bumped tree | workspace **2,331/0** · acceptance **23/23** · clippy `-D warnings` clean · fmt clean · naming-guard 18 frozen · architecture 27 packages |
| Bump commit | `f4a2eea` pushed to `main` |
| CI on `f4a2eea` | **4/4 success**: pull-request-validation, msrv, web-driver, five-target-foundation (~34 min) |
| Tag | `v0.23.0` created **after** all four green; annotated; pushed |
| Release run | `completed success` on `f4a2eea` |
| Public discovery | `/releases/latest` → **v0.23.0**, draft=false, prerelease=false, **16 assets** (5 platform archives + checksums, web-driver images) |
| Registry PR #539 | updated **in place** on `agent-vesper/v0.20.51` (never closed-and-replaced): manifest 0.22.9 → 0.23.0 at `e3b9317`, byte-identical read-back, retitled "Add Agent Vesper v0.23.0" |

## Contracts held

- Tag preceded by green gates on the exact commit; no tag-first.
- Registry PR updated in place; branch name unchanged (historical).
- No local installation run; Alex owns the update step.
- No live provider calls in verification.

## Unresolved

- Real-device acceptance of the enrollment UX (enable acceptance →
  status lines within seconds; ≤5 min visible worst case) remains
  Alex's.
