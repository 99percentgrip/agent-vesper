# v0.22.6 release execution

Status: released and verified within the documented automated scope.

## Objective

Publish the [microphone and long-dictation implementation](voice-control-execution.md)
under the [voice PRD](../voice-control-prd.md), preserving Alex's local installation,
skill library and separate GLM checkout.

## Methods and files

Bump the workspace, internal exact dependency pins, workspace lockfile packages,
registry manifest/URLs and pinned installation examples from 0.22.5 to 0.22.6.
Push the version commit to main. Require successful canonical, MSRV, five-target
foundation and contained web-driver workflows for that exact commit before tagging.
The immutable tag triggers the five-platform compiled/checksummed release. Verify
published assets and update existing ACP registry PR #539 in place.

## Evidence and unresolved items

Local `cargo xtask verify`, focused renderer/worker tests, ten-minute PCM slicing,
and timed real-terminal lifecycle checks passed; scope and missing device acceptance
are recorded in the implementation report. Both hosts passed locked all-feature
version checks. Remote receipts are recorded below.
No installer is run on Alex's installation. Real-microphone/macOS acceptance remains
unexecuted and is disclosed in release notes. No release claim supersedes that gap.

## DOX and readiness

Foundation owns this release ledger and the evidence index links it. Manifest-only
version changes do not change owning crate/registry contracts; those docs remain
unchanged. README and installation guide describe the new controls and recovery.

## Exact-commit receipts

Version commit/tag target: `81884c68728e392e26415a11623aabd12597412b`.
The tag was pushed only after all four successful `push` runs were rechecked:

- [Canonical](https://github.com/99percentgrip/agent-vesper/actions/runs/34752624278): workspace verification, 23 acceptance cases, mutation checks, installer preservation, and new microphone-free terminal/PCM cases passed.
- [MSRV 1.88](https://github.com/99percentgrip/agent-vesper/actions/runs/34752624268): workspace, contracts and exact acceptance passed.
- [Five-target foundation](https://github.com/99percentgrip/agent-vesper/actions/runs/34752624259): Linux x86-64/ARM64, Intel/Apple Silicon macOS and Windows x86-64 all passed.
- [Contained web driver](https://github.com/99percentgrip/agent-vesper/actions/runs/34752624274): both image architectures and namespace lifecycle passed.
- [Release packaging](https://github.com/99percentgrip/agent-vesper/actions/runs/34754194892): all five builds and publication passed.

Canonical logs explicitly record ten-minute PCM slicing and the >90-second real
terminal transcription lifecycle passing. These are controlled inference fixtures,
not physical microphone or real-model speech-accuracy evidence. Local logs are
`/tmp/vesper-0226-{canonical,msrv,platform,driver}.log`; the linked remote runs are
the durable receipts. Relative Markdown links and registry schema/icon validated.

## Publication

[v0.22.6](https://github.com/99percentgrip/agent-vesper/releases/tag/v0.22.6)
is public and stable with 16 nonempty assets. User-facing release notes document
record/stop, long dictation, cancellation, recovery and device-acceptance limits.
Existing [registry PR #539](https://github.com/agentclientprotocol/registry/pull/539)
was updated in place on `agent-vesper/v0.20.51` at
`d913ef4ddfa6632eec8fee534e77fd1af9b1622d`; remote manifest and icon byte-match
canonical files and pass the upstream validator. This is a PR update, not an
upstream merge claim. Public-download checksum and binary-version verification passed.

## Final verification and readiness effect

- Stable latest-release discovery resolves v0.22.6; the exact expected set of 16
  named binary/checksum/image assets is present and nonempty.
- Downloaded `agent-vesper-acp-linux-x86_64.tar.gz` from the public release and
  matched its SHA-256 against the published checksum. Extracted only ACP/TUI into
  `/tmp/vesper-0226-assets`; `--version` reports 0.22.6 for both. No installer ran.
- Alex's 488 skill-library files, five protected GLM files, original checkout HEAD
  and ten preexisting dirty files match their recorded preservation baselines.
- README, installation guide, PRD and formal implementation/release records are
  updated. Relative links and whitespace pass. Post-release receipt edits are
  documentation only; the immutable release remains on the fully gated commit.

The update is available through native Check for updates. Alex's installed copy
was not replaced. Real-device microphone capture, native macOS microphone permission
flows and speech-accuracy acceptance remain unexecuted; Windows capture remains
unsupported. No passing synthetic lifecycle test is substituted for those checks.
