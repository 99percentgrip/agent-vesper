# v0.22.6 release execution

Status: prepared; exact-commit remote gates and publication pending.

## Objective

Publish the [microphone and long-dictation implementation](voice-control-execution.md)
under the [voice PRD](../voice-control-prd.md), preserving Alex's local installation,
skill library and separate GLM checkout.

## Methods and files

Bump the workspace, internal exact dependency pins, workspace lockfile packages,
registry manifest/URLs and pinned installation examples from 0.22.5 to 0.22.6.
Push the version commit to main. Require successful canonical, MSRV, five-target
foundation and contained web-driver workflows for that exact commit before tagging.
The immutable tag triggers the five-platform compiled/checksummed release. Verify
published assets and update existing ACP registry PR #539 in place.

## Evidence and unresolved items

Local `cargo xtask verify`, focused renderer/worker tests, ten-minute PCM slicing,
and timed real-terminal lifecycle checks passed; scope and missing device acceptance
are recorded in the implementation report. Both hosts passed locked all-feature
version checks. Remote receipts will be recorded after the workflows complete.
No installer is run on Alex's installation. Real-microphone/macOS acceptance remains
unexecuted and is disclosed in release notes. No release claim supersedes that gap.

## DOX and readiness

Foundation owns this release ledger and the evidence index links it. Manifest-only
version changes do not change owning crate/registry contracts; those docs remain
unchanged. README and installation guide describe the new controls and recovery.
