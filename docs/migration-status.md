# Migration Status

Last updated: 2026-09-25

VRO-18 native xAI provider is at **PR-4 COMPLETE; PR-5 NEXT**. The dedicated
`vesper-provider-xai` leaf now owns Vesper-managed API-key credentials, fixed
Responses dispatch, strict shared-function serialization, bounded SSE decoding,
cancellation, safe errors, usage normalization and opaque reasoning. Offline
red/green evidence is 29/29; architecture and strict Clippy pass. The adapter is
not registered or advertised, and no live provider call occurred. Dynamic
discovery/capability intersection now covers eight verified families,
current aliases, US endpoint bounds, model-specific reasoning/multi-agent rules
and strict schemas. Native browser/device Grok session auth, refresh/logout,
billing isolation and subscription-proxy routing now pass offline loopback
evidence. Explicit stored continuation, stable prompt-cache routing, citation
retention, opaque reasoning and normalized usage now pass offline fixtures.
Hosted tools, native compaction/WebSocket, hosts and release remain open. Evidence:
`docs/architecture/recon_xai_native_provider.md`,
`docs/foundation/xai-provider-recon-execution.md`, and
`docs/foundation/xai-provider-pr1-execution.md`,
`docs/foundation/xai-provider-pr2-execution.md`, and
`docs/foundation/xai-provider-pr3-execution.md`, and
`docs/foundation/xai-provider-pr4-execution.md`.

VRO-17 voice-quality addendum (2026-09-24): the remaining short Kokoro
hiccup is repaired at its measured owner. The real hygiene→worker path showed
0.972 s of stacked model padding at a two-sentence short-reply boundary; guarded
short-unit-only cleanup reduces it to 0.200 s for both installed voices while
the paced long-form and exact continuity PTY receipts remain green. Settings now
labels the optional capability “Live transcript preview” and makes clear that
final text still appears after Stop. Evidence and candidate identity:
`docs/foundation/voice-short-reply-quality-repair.md`. Alex subsequently
accepted the candidate as “Looks like smooth” on the tested setup, and the
pronunciation repair remains accepted with no new regression reported.

R6 is closed on bounded real-device evidence: one-press and repeated barge-in,
old-speech stop/no-resume, exactly one replacement turn, explicit Stop with no
accidental capture, and later-F9 recovery passed on the tested setup. The tested
Ctrl+C cancellation message was acceptable. R1–R20 are now closed at their
approved scopes. PR-5 is now in progress: ACP's protocol-backed voice exclusion,
cross-host provider/runtime parity, user docs, cloud future-feature contract and
release voice-feature matrix are implemented. Exact-commit verification and
publication remain before VRO-17 closes.

| Area | Status | Current evidence |
| --- | --- | --- |
| Reconnaissance, contracts, and frozen oracle | COMPLETE | `docs/recon/`, pinned Python oracle `bf4d4287e2e3320aa3f09015f678e6169d520045` |
| Provider-neutral domain/runtime/agent harness | IMPLEMENTED | Typed provider registry, session supervisor, multi-turn agent loop, permission gate, progress port, hosted tool service, result-aware loop detection, truthful incomplete-terminal classification, autonomous plan continuation, transactional interruption outcomes, a bounded catalog-backed full-payload capability gate, and token-aware transactional semantic compaction with complete-tool-transaction retention and persisted quality lineage shared by ACP/TUI |
| Native implementation acceptance | IMPLEMENTED; LOCAL VERIFICATION | ADR 0028: enrolled PRD/project-rule coverage, independent review, exact-test receipts, bounded repair, both-host publication and native Settings. Enrollment-visibility repair (2026-09-14): nested reviewer stage lines stream to both hosts, contract ladder capped at 2 with a 300 s enrollment ceiling, refusal/failure is one loud actionable outcome. Evidence: `docs/foundation/completion-assurance-execution.md`, `docs/foundation/acceptance-enrollment-visibility-execution.md`. Remote CI/release/install are separate gates. |
| Z.ai GLM production adapter | IMPLEMENTED | Evidence-backed eight-model catalog shared by ACP/TUI, including GLM-5.3-Flash native image input (URL/Base64), 1M context, 128K output, model-specific reasoning and plan gates, Coding/Standard/BigModel endpoints, auth, streaming tools/reasoning/usage, interruption classification, bounded safe continuation, no-replay ambiguous-tool handling, and validated auxiliary-model routing for tool-free compaction with main-model fallback |
| ACP executable | IMPLEMENTED | Full harness is the production default; mixed and image-only ACP content is preserved; capability failures name the active model and catalog-verified alternatives through the existing selector; live permissions, persistence, setup/install, native plans, actionable failures, slash parity, and streaming tool correlation remain implemented |
| Persistence, memory, checkpoints, MCP, plugins, workers | IMPLEMENTED | Durable bounded stores and typed hosted/slash-command bridges; no SQLite in checkpoints; unsigned plugins compile only in debug |
| Native TUI command surface | IMPLEMENTED | 102 default registered routes (103 with Swarm) including distinct `/export last`; no deferred fallback; missing routes fail closed |
| Native TUI interaction surface | IMPLEMENTED AND LOCALLY VERIFIED | Full palette/value pickers, mouse-operable footer/rows, reasoning/activity/TODO/report panels, working-tree views, Vim, keybinds, clipboard images, and explicit catalog-backed capable-model consent that preserves/resumes the multimodal turn; sound, QR approvals, and optional local voice remain implemented |
| Packaging/install/uninstall | IMPLEMENTED AND LOCALLY VERIFIED | Cross-platform scripts; checksum-verified local release installation for both binaries; release archives bundle the 93-skill seed library seeded non-destructively by both installers |
| Frozen-oracle implementation parity gate | PASSED LOCALLY | `docs/parity-audit-report.md`; AST command audit, `cargo xtask verify`, release structural audit, installer smoke, Cargo Deny, and RustSec audit are green |
| VRO-13 QM extraction (firewall, sandbox, scopes, daemon) | COMPLETE | `docs/qm-extraction-prd.md`; PR-1..PR-8 landed. Hard-denial `CommandFirewall` wired once into `RunCommand` (deny outranks every permission mode incl. Bypass); opt-in `vesper-sandbox` (Linux namespaces + feature-gated Docker) behind fail-closed capability gates; `WorkspaceScope` stamp-pinned identity with layered skills and deny-precedence firewall composition; exactly-once cron slot claims, single-writer `daemon.lock`, and the bounded watcher sweep with rate limits. Cross-feature end-to-end fixture: `crates/vesper-harness/tests/vro13_e2e.rs` (watcher fire → bounded turn → composed firewall → sandbox route → scope-keyed transcript; 5 tests, 6 under `--features docker`, both green) |
| VRO-14 web oracle extraction | COMPLETE — v0.20.89 | Shared TUI/ACP contained pipe-CDP sessions, one-step render escalation, sitemap discovery and bounded fetch/crawl passed production acceptance. Exact-commit canonical/supply-chain, MSRV, five-target and dual-architecture image CI passed before tagging; five application bundles and both pinned driver archives are public. Registry PR #539 was updated in place and awaits upstream review. `docs/foundation/vro14-gap-audit.md` records evidence and implementation choices; `docs/web-tools.md` owns opt-in deployment. |
| Additional production providers | IMPLEMENTED | LM Studio and native OpenAI are registered alongside Z.ai. OpenAI has API-key/subscription authentication, Responses streaming, tools, images, model/effort controls, and native memory extraction in both hosts; see `openai-provider-prd.md` for gates and explicit limits |
| VRO-18 native xAI provider | PR-4 COMPLETE — PR-5 NEXT | API-key Responses plus native browser/device Grok session auth, locked refresh/logout, strict billing isolation, first-party subscription proxy, shared function/tool-result continuation, explicit stored response continuation, prompt-cache routing, opaque reasoning, usage and citations pass offline coverage (29/29). Hosted tools, compaction/WebSocket, hosts, live acceptance and release remain open. No xAI adapter is registered or advertised. |
| Context compaction parity | IMPLEMENTED AND LOCALLY VERIFIED | ADR 0023: active-model token pressure at 60/75/85%, automatic compaction at 85% with response reserve, semantic `/compact [focus]`, immutable system prompts, complete recent tool transactions, secret-scrubbed untrusted summary input, auxiliary→main→deterministic fallback, transactional rollback/fail-closed overflow, ACP/TUI persistence, VRO/direct parity, full TUI display transcript retention, and persisted quality-regression telemetry |
| Skill orchestration parity | IMPLEMENTED AND LOCALLY VERIFIED | ADR 0024: provider-neutral metadata discovery/ranking, policy/tool/platform/risk eligibility, automatic top-three composition, explicit `/skill` and bundles, transient bounded bodies, isolated-worker contexts, direct/VRO/ReAct and ACP/TUI parity, outcome feedback, and compaction-safe audit identity |
| Model-assisted skill routing | IMPLEMENTED — OPT-IN PREVIEW / QUALITY HOLD | Settings → Skills enables bounded configured-provider selection in TUI and ACP; Standard remains default and source skills are preserved. The 205-case inspected live regression records 89/95 positive recall, 0/60 no-skill activations and 2/30 forbidden-sibling selections, including five provider fallbacks. See [implementation evidence](foundation/skill-routing-model-assistance-execution.md), [live evaluation](foundation/skill-routing-model-live-evaluation.md) and [release execution](foundation/v0.22.4-release-execution.md). This does not close the full routing-quality or pending score-floor gates. |
| VRO-15 swarm oracle extraction | IMPLEMENTED — DEFAULT OFF / EXACT-COMMIT RELEASE GATED | ADR 0026 supersedes ADR 0025's completion, single-stream adapter and TUI-only claims. Provider-neutral foundations, native AgentLoop/tool composition, independent pooled execution, immutable ledger snapshots and local regression gates are present. Asynchronous lease ownership and real contained native worker composition/cleanup are locally verified. Persisted Settings and both-host native runs now have local acceptance, including real provider/embedding loopback transport, scoped tools and retained histories. Isolated and shared container scopes have real confinement/cleanup evidence; native namespace execution now also passes after repairing outer-ID mapping, private-root confinement, environment encoding and bounded output waits. Final local canonical/MSRV (2072 tests each), default workspace (2015), supply-chain, real namespace and browser feedback, and four scale gates pass. Candidate CI validates both Docker architectures and native hosts; final release acceptance is enforced by successful exact-commit canonical/MSRV/five-target/web-driver workflows before tagging. `docs/foundation/vro15-repair-execution.md` owns the current F01–F18 reconciliation; `docs/foundation/vro15-gap-audit.md` is the historical audit. Activation remains an explicit native Settings Save; release notes carry final workflow and installation evidence. |
| VRO-16 advanced hive governance | COMPLETE — RELEASED IN v0.21.9 | All three PRs implemented, locally verified, and then re-audited end to end. The final audit (`docs/foundation/evidence-index.md` VRO-16 entry) found nine integration gaps of the VRO-15 decorative-layer class — engines tested in isolation but not composed — and repaired all of them in the same pass with executing proofs in `crates/vesper-swarm/tests/hive_governance_audit_fixes.rs`: G1 review panel now composes pre-synthesis on governance hives (bare hives keep exact VRO-15 turn counts); G2 gate publication uses a real `governor` bus inbox with loud failures (previously `Err(UnknownSubscriber)` was silently discarded — the tier carried zero traffic); G3 every gate resolution persists to the ledger audit trail before state effects (Cancel previously dropped its own record) and audit entries round-trip as `AuditEvent`; G4 `governance: gated` enforces decomposition+synthesis boundary gates (previously an advertised no-op); G5 Redirect directives reach re-dispatched worker prompts; G6 budget exhaustion renders in both hosts; G7 `GuidanceInjected` removed from D4 by amendment (directive rides `GateResolved.command`); G8 evidence-index updated; G9 SmartPause consumes the real watchdog percentage. A same-day
re-audit (owner challenge) found the G4 boundary gates opened without
pausing the gated work plus a hot-loop in `run_to_completion`; both
boundaries now park and resume on host resolution/expiry with
behavior-based proofs (zero driver prompts while open; no spin on an
open gate). PR-1: D1 bounded gates (injected-clock expiry, `FailTask` fallback), D2 task-level `Suspended` (barrier parallelism, no hive lock), shared `HostCommand` surface, restart countdown proof, native settings round-trip. PR-2: bounded PIVOT/REFINE (caps 3/2, audited exhaustion, dangling-rationale fail-closed), `VersionRegistry` retrieval proof, frozen-snapshot panel isolation, D3 authored-surface `template_identity` (relabeling refused). PR-3: digest gate (tamper-inadmissible), trace gate (`[evidence:<id>]` fail-closed), budget watchdog (50/80 emit, 100% hard-stop, once each), full-pipeline audit chain, per-host surface proofs. Naming guard: 11 tokens, monotonic-count assertion, 18 frozen hits, zero growth. Evidence baseline: workspace all-features **2185/0**, default **2113/0**, clippy `-D warnings` clean, fmt clean, architecture 27 packages, `cargo xtask acceptance` 20/20 (2026-09-12, final-audit closeout). Release evidence (v0.21.9, tag `v0.21.9` at `0b4cd0d`): all four push workflows green on the exact commit before tagging — canonical CI (run 34674795540), MSRV (34674795595), five-target foundation (34674795554), dual-architecture web-driver including the real Hive/native-process/TUI gates (34674795541); release run 34676270024 verified exact-commit CI, built all five platforms and published 16 assets (five archives + SHA-256 sidecars + both Linux driver images with immutable IDs). Archive checksums verified locally; both binaries report 0.21.9; installer upgrade-in-place preserves user state. The web-driver gate initially rejected the first version commit `b790e4f` — the native loopback fixtures did not know the newly composed review-panel/decision turn shapes; repaired by making review turns structurally tool-free (`TaskKind::Review`) and teaching the fixtures, with the real CI-built image reproducing all gates locally via podman before the fix commit. Registry PR #539 updated in place on the same branch to v0.21.9. |
| Advanced context paging (skill chunks + rich routing metadata) | COMPLETE — all five PRs landed; D3 verdict ADOPT recorded | `docs/advanced-context-paging-prd.md`; recon provenance `docs/architecture/recon_context_paging.md` (context upstream, MIT, pinned commit recorded there). PR-1 chunk storage/manifest/fail-closed caps (32 chunks, 24,000 B) + G5 proofs (`chunk_store.rs`, 12 tests). PR-2 two-level routing, `MAX_CHUNKS_PER_SELECTION = 3`, budget fail-closed (`skill_routing.rs`, 8 tests). PR-3 transient `<agent-vesper-skill-chunk>` composition, AC-3 transience/parity via the shared seam (`vesper-harness/tests/context_paging_composition.rs`, 6 tests; zero host code changed). PR-4 strict three-condition ablation → **ADOPT**, `CHUNK_METADATA_ROUTING_ENABLED = true` (`docs/foundation/context-paging-pr4-eval.md`; audit-corrected overhead 13–14/16). PR-5 authoring guidance (`skills/AGENTS.md`, `vesper-skill-authoring` v1.1.0). Zero-regression guarantee maintained end to end (G5 proofs in PR-1/2/3). No user-visible surface change. Execution records: `docs/foundation/context-paging-pr{1..5}-*.md` + `context-paging-full-audit.md` (5 audit findings repaired with fail-then-pass regressions; ADOPT re-derived and stands). |
| Ranker hardening (chunk-tier alias cross-talk) | COMPLETE — RELEASED IN v0.22.0; audit fix (A1 metric tokenizer) RELEASED IN v0.22.1 (four workflows green on 1b221dc; binary verified; local install updated). Evidence: `docs/foundation/v0.22.1-release-execution.md`. Original release evidence: | All three PRs implemented and locally verified (floor 2220/0, acceptance 20/20, naming-guard clean). PR-1 anchored the defect (cross-talk pin fails for exactly the audited reason: rollback displaces migrate on 1,560 manufactured points; no-alias control green). PR-2 decoupled the chunk tier (`raw_semantic_tokens` — raw stemmed chunk pools, prompt pool keeps expansion; skill tier byte-identical per M5). PR-3 re-eval stop rule clean: canonical D3 table and overhead numbers identical under the hardened ranker (controls 0/13–14/16 across conditions; probe ladders unchanged; verdict re-derived ADOPT, 2 families) — the recon's zero-blast-radius finding held empirically. Release v0.22.0 per the exact-commit contract: version commit 9b56a9e, four push workflows green on that exact commit (runs 34691160789 / 34691160751 / 34691160754 / 34691160884) before tagging; release run 34691968917 published 16 assets across 5 platforms; binary reports 0.22.0; registry PR #539 updated in place (head 55973fe). Evidence: `docs/foundation/ranker-hardening-pr{1,2,3}-execution.md`. | `docs/ranker-hardening-prd.md`; recon provenance `docs/architecture/recon_alias_crosstalk.md` + `docs/foundation/recon-alias-crosstalk-execution.md`. Scope fenced to recon Option A (raw stemmed chunk pools — `rank_chunks` bypasses the alias loop; skill tier untouched) + Option D (failing cross-talk fixture anchors the defect before the fix). Incident basis: PR-4 `deploy-runbook` slug routed the `rollback` chunk on 1,560 manufactured overlap points (`skill_orchestrator.rs:869/:873`; recon §1.2 corrected the first-recorded 520). Blast radius of the fix proven zero over the shipped floor (recon §3). Bounded residual (520-pt prompt-side literal match) recorded as accepted. Three PRs: anchor fixture → decoupling → re-eval under the hardened ranker with D3-verdict stop rule. |
| Chunk score floor (cosine noise floor) | COMPLETE — PR-1+PR-2+Q1+PR-3 landed 2026-09-13 | PRD `docs/chunk-score-floor-prd.md`. Conjunction gate `(overlap>=1 || name_match) && score>=MIN_CHUNK_ROUTING_SCORE (520)` live in `rank_chunks`; delimiter-bounded chunk-name admission (`chunk_name_matches`, Q1). G5 restored to the literal zero-chunks form with a corrected vocabulary-free fixture (original prompt overlapped phase4 via `finish`); non-vacuity receipts on the pre-PR-2 tree (3 noise chunks routed → literal fail). Skill tier byte-identical across the initiative (`AUTO_ACTIVATION_SCORE` untouched; `git diff 5cf5835~1..HEAD` shows only the two chunk-tier lines). Final: floor 2,258/0 failed, acceptance 23/23, clippy/fmt/naming-guard clean. Executions: `foundation/chunk-score-floor-pr1/pr2/q1/pr3-execution.md`. |
| VRO-17 voice-oracle extraction (real-time bidirectional voice) | **R1–R20 CLOSED; PR-5 RELEASE GATES IN PROGRESS** | CPU and verified FLM/NPU STT routes are production-implemented and user-accepted at their documented scopes; CPU Kokoro is the accepted synthesis route. Concrete cloud STT/TTS are future optional `VoiceStt`/`VoiceTts` features and no provider is advertised in v1; NPU TTS remains capability-gated and absent. R4 is closed as an optional STT capability with truthful final-only Settings; R20 uses the managed capture store in default and feature builds. R6 is PASS on Alex's tested setup. Short-reply quality and pronunciation are user-accepted at that bounded scope. ACP truthfully advertises `audio=false` and documents microphone/playback/status/Settings exclusions while sharing generic provider/runtime cancellation semantics. The release workflow now compiles the TUI Kokoro/FLM features; exact-commit CI, tag, publication, assets/checksums and registry update remain. Evidence: docs/foundation/voice-pr5-host-release-execution.md and docs/foundation/voice-vro17-final-completion-audit.md. |
| Completion-reporting workflow (productized) | IMPLEMENTED | Shared `COMPLETION_REPORTING_INSTRUCTION` mandate in `vesper-harness`, injected by both hosts at every agent-loop build (ACP interactive + worker, TUI direct + tool-loop); cross-host parity enforced by prompt-composition tests in both apps. Methodology ships as the `work-unit-reporting` seed skill (library 94→95, bundle `software-development`), seeded into every fresh install non-destructively per `scripts/AGENTS.md`. Implements: file report + in-chat summary per work unit, honest-evidence rules, regression-first final audits, and release-gate discipline (`verify-with-xtask-verify` companion skill). |

The prior Stage 4/5-only text in this file was obsolete: the workspace now
contains the complete agent loop, hosted tools, persistence writers, memory,
checkpoints, MCP/plugins, worker composition, ACP harness, and native TUI.

“Multi-provider” describes both the provider-neutral architecture and the three
currently registered real adapters: Z.ai, LM Studio and native OpenAI. Any further provider
becomes production support only after a real adapter supplies its own
authentication, endpoints, catalog, capabilities, request/stream mapping,
fixtures, and CI evidence.
